# Bitle OTA partition layout (4 MB flash).
# nvs and phy_init keep the stock offsets so identity keys, nickname and
# clock state survive the migration. fw_manifest holds the 110-byte signed
# release manifest, flashed alongside the image so a wire-flashed node can
# serve its own firmware to peers without any BLE push tool.
# Name,        Type, SubType, Offset,   Size
nvs,           data, nvs,     0x9000,   0x6000
phy_init,      data, phy,     0xf000,   0x1000
otadata,       data, ota,     0x10000,  0x2000
fw_manifest,   data, 0x40,    0x12000,  0x1000
ota_0,         app,  ota_0,   0x20000,  0x1C0000
ota_1,         app,  ota_1,   0x1E0000, 0x1C0000
# Store-and-forward mailbox: sector-ring of carried packets (courier
# envelopes). Fills the remaining 384 KB of the 4 MB part exactly.
msgstore,      data, 0x41,    0x3A0000, 0x60000
